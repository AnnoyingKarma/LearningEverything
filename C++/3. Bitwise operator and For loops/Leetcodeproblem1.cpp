#include <iostream>
#include <math.h>
using namespace std;

int main()
{
/*

// revese the integer if it overflows return value 0 .. Leetcode


    int n , ans = 0;
    cin >> n;
    
    while(n!=0)
    {
        int digit = n%10;

        if((ans > INT_MAX/10) || (ans < INT_MIN/10))
        {
            cout << 0 << endl;
        } 

        n=n/10;
        ans = (ans * 10) + digit;
        
    }
    cout << ans << endl;
    

// Complement of Base 10 integer .. Leetcode

int n, mask = 0;
cin>>n;
int m = n;

while (m!=0)
{
    mask = (mask << 1) | 1;
    m = m >> 1;
}
int ans = (~n) & mask;
cout << ans;


int n;
cin >> n;

for (int i=1; i<31; i++)
{
    int ans = pow(2, i);
    if (n == ans)
    {
        cout << "true";
    } 
        cout << "false";
    
}
*/

}