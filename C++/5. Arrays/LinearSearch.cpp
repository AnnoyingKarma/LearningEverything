#include <iostream>
using namespace std;

/*
// linear search to find a key..
bool search(int ary[], int size, int key)
{
    for (int i = 0; i<size; i++)
    {
        if(ary[i] == key)
        {
            return 1;
        }
    }
    return 0;
}


// linear serach (Done by me) to find if the value is present in array.
bool check (int ary[], int size, int key)
{
for (int i = 0; i<size; i++)
{
    if (key==ary[i])
    {
        return 1;
    }
}
return 0;
}

*/

int main ()
{
/*
    // Done by me

    int arr[10] = {3, -1, 45, 31, 87, -10, 2, 9, 3, 1};
    int len = sizeof(arr)/sizeof(int);

    for (int i=0; i<len; i++)
    {
        if (arr[i] == 1)
        {
            cout << "\n" << "There is 1 in this array on " << i << "th index." << "\n" << endl;
        }
    }


// liner search to find a key.
    int arr[10] = {3, -1, 45, 31, 87, -10, 2, 9, 3, 1};

    cout << "Enter the element to search for " << endl;
    int key;
    cin >> key;

    bool found = search(arr, 10, key);

    if ( found ) {
        cout << "Key is present " << endl;
    }
    else {
        cout << "Key is absent" << endl;
    }
    

// Linear search (done by me ) to find if value is present in array
int n;
cin>> n;
int arr[10] = {3, -1, 45, 31, 87, -10, 2, 9, 3, 1};

bool lastcheck = check(arr, 10, n);

if (lastcheck)
{
    cout << "The key is present" << endl;
}
else
{
    cout << "The key is absent" << endl;
}
*/

}