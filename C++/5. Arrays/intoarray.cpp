#include <iostream>
using namespace std;


/*

// function of array for integer
void intarray(int ary[], int size)
{
        for (int i = 0; i<size; i++)
    {
        cout << ary[i] << endl;
    }
}


//function of array for character
void chararray(char ary[], char size)
{
    for (int i = 0; i<size; i++)
    {
        cout << ary[i] << endl;
    }
}
*/






int main()
{



/*

//Initialization of array

    int singledigit[3] = {3,5,6};

//Accesing an element in array
    cout << endl << singledigit[2] << endl << endl;

// making another array without providing all values    
    int third[15] = {6,1};
    for (int i = 0; i<15; i++)
    {
        cout << endl << third[i] << endl;
    }

// initializing all array with 0;
// provide a single value of zero for the first array-- eg -- one[0]=0
// it will automatically provide value of zero to other array element which we dont provide value to
// if we dont provide all the value to array it will change the value to 0 of the elemets which were not provided with a value
    int one[15] = {0};
    for (int i = 0; i<15; i++)
    {
        cout << endl << one[i] << endl;
    }

// using function to print array 
    int n = 10;
    int zero[10] = {1,2,3,5,2,25,6,2};

    intarray(zero, n);

// getting size of array
// divide size of array that will be - if array size is 9 it will be 9*4 = 36 so we should divide it with sizeof(int) or sizeof(1) to make it 9;

    int forth[9] = {2,3,34,5,35,2,52,5,2};
    int forthsize = sizeof(forth)/sizeof(int);
    cout << forthsize << endl;

// array of char
char ch[3]={'@','1','A'};
chararray(ch, 3);

*/



}