#include <iostream>
using namespace std;

int main ()
{
    /*
    int n;
    cin>>n;
    int a[n]; // its showing error but it will work
    for(int i=0; i<n; i++)
    {
        cin>>a[i];
    }
    int sum=0;
    for(int i=0; i<n; i++)
    {
        sum = sum+a[i];
    }
    cout << sum;

    // 
    int b; cin>>b;
    int b[4][3];// 4size ke array ke 3 copy or bna do 
    // ^^^ this is 2d array 
    for (int i=0; i<4; i++)
    {
        for(int j=0; j<3; j++)
        {
            cin>> b[i][j];
        }
    }
        for (int i=0; i<4; i++)
    {
        for(int j=0; j<3; j++)
        {
            cout>> b[i][j] << " ";
        }
        cout << "\n";
    }
    */

   // array maximum memory locally size 10^5
   // 10e5 you can't declare more than 10e5 array
   // it will give you segmentation fault
   // you can define bigger array globally but make it constant like this
   // const int n=2e7;
   // 10e7 globally for online competitions


}

